/* Este bloque queda vacío a propósito — ver README.md. La lógica del widget
 * ahora se carga como script externo desde una sola etiqueta <script> sin
 * contenido de texto, incluida al final del bloque HTML, para evitar que el
 * editor de WordPress corrompa el JavaScript inline (wpautop). */
