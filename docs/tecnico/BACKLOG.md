# Backlog — EstetiCAN
### Registro de Trabajo Pendiente

> **Uso:** Este archivo es la fuente de verdad del backlog. Actualizar en cada sesión.
> Cuando un ítem se completa, moverlo a la sección **Completados** con fecha.
> Los ítems en **Activos** son los candidatos para el próximo sprint.

---

## Activos

### Prioridad Alta — Seguridad y Estabilidad

| ID | Ítem | Tipo | Notas |
|---|---|---|---|
| BL-006 | ~~Bloquear endpoint `/up`~~ **COMPLETADO** — movido a sección Completados | Seguridad | |
| ~~BL-007~~ | ~~Verificar Transform Rules Cloudflare: X-Frame-Options, Referrer-Policy, Permissions-Policy~~ **COMPLETADO** | Seguridad | Headers en nginx de mob; backoffice ya los tenía vía middleware Laravel |
| BL-028 | Diseñar estrategia de defensa por firewall (ufw) para el servidor OPi | Seguridad | Estado actual verificado 03/07/2026: ufw y fail2ban activos/habilitados (jail sshd: maxretry=4, findtime=10min, bantime=1h); SSH permite password además de pubkey (`PasswordAuthentication yes`); puerto 22 y SMB (139/445) escuchan en `0.0.0.0` en ambas LAN; Portainer/NPM admin ya restringidos a `192.168.100.250`. Definir reglas ufw explícitas por servicio/red y evaluar desactivar auth por password en SSH |
| ~~BL-066~~ | ~~Borrado de usuarios: validar dependencias antes de eliminar~~ **COMPLETADO** | Bug/Seguridad | Ver Completados |
| ~~BL-068~~ | ~~Corregir `onclick=`/`onsubmit=` inline restantes (NT-042)~~ **COMPLETADO** | Bug | Ver Completados |

### Prioridad Alta — App Móvil (operador)

| ID | Ítem | Tipo | Notas |
|---|---|---|---|
| ~~BL-020~~ | ~~Breadcrumbs en todas las pantallas vía ScreenHeader~~ **COMPLETADO** | Feature | |
| ~~BL-023~~ | ~~App móvil: Selector de operador + agenda individual + renombrar Groomer→Operador~~ **COMPLETADO** | Feature | screenTags MobOpPkr/MobOpAg; breadcrumb dinámico; compresión de fotos |
| BL-024b | WhatsApp Fase 2: confirmación de cliente, CRM completo, bandeja de apertura/cierre diaria con doble vía | Feature/CRM | Fase 1 completada (ver Completados). Alcance futuro: recepción de respuestas, historial conversacional |

### Prioridad Media — Funcionalidad de UI y Configuración

| ID | Ítem | Tipo | Notas |
|---|---|---|---|
| BL-001 | Tema de UI: reparar persistencia y cambio reactivo de paleta de colores | Bug/Feature | El tema se pierde al recargar o no se aplica reactivamente |
| BL-002 | Favicon & Empresa: subida de favicon + datos generales del negocio | Feature | `SystemSetting` — branding básico |
| ~~BL-003~~ | ~~Email Avanzado: credenciales SMTP (usuario/password, puertos, SSL/TLS)~~ **COMPLETADO** — cumplido por BL-040 (ver Completados) | Feature | |
| BL-004 | Zonas Horarias: reemplazar selector UTC por selector completo | Feature | Selector actual solo muestra offsets UTC, no nombres de zona |
| BL-047 | Módulo Clínico Veterinario — Fase 2 (diferida a propósito, ver BL-046 en Completados). Pendiente: espejo automático "alergia severa → `pet_medical_alerts`", soporte en app móvil, catálogo real de `icd_code`. ~~`clinical_attachments` real~~ **COMPLETADO** 21/07/2026, ~~PDF/impresión oficial del expediente y receta~~ **COMPLETADO** 24/07/2026 — ver Completados | Feature | El módulo sigue apagado por defecto (`clinical_module_enabled=false`) hasta que el usuario decida arrancar ese lado del negocio |
| BL-053 | Artículos de uso interno / equipo para servicios (máquinas, rasuradoras, herramientas) — concepto nuevo, distinto de `items` (catálogo de venta, BL-050/051) y de `Resources` (jaulas/cuartos vía `ResourceAllocation`, ya existe). Sin diseñar: ¿necesita mantenimiento/vida útil?, ¿se asigna a un operador?, ¿bitácora de uso? | Feature | Anotado el 15/07/2026 durante la conversación de reorganización de navegación (Inventario/RH); no se ha definido ni el modelo de datos ni si amerita su propio módulo de navegación |
### Prioridad Alta — Módulo Financiero (sprint actual)

| ID | Ítem | Tipo | Notas |
|---|---|---|---|
| ~~BL-022~~ | ~~App móvil: gestión de caja — sesión activa, registrar movimientos, balance de movimientos~~ **COMPLETADO** | Feature | Ver NT-011 sobre filtro sucursal en cobros |
| ~~BL-021~~ | ~~Migrar registros históricos de `cash_ledgers`/`bank_ledgers` a asientos contables~~ **COMPLETADO** | Mantenimiento | Comando `finanzas:migrar-ledgers-historicos`; idempotente; ejecutado en prod |

### Prioridad Baja — Nuevas Capacidades

| ID | Ítem | Tipo | Notas |
|---|---|---|---|
| BL-008 | Reportes PDF: diseño e impresión de presupuestos, órdenes de trabajo y facturas | Feature | Evaluar `barryvdh/laravel-dompdf` o Browsershot |
| BL-012 | App clientes (futura): autoregistro de clientes — app pública separada | Feature | No es `mob_apps/operador` |
| BL-071 | Formulario/vademécum veterinario estructurado — hoy la dosis de una receta es texto libre (`clinical_prescription_items.dose`) y los medicamentos de Farmacia solo tienen nombre/marca/presentación (`items`, sin campos clínicos). Propuesto por el usuario 24/07/2026: tabla nueva ligada a `items` con `principio_activo`, `concentracion`, `forma_farmaceutica`, `especie`, `via`, `mg_kg`, `frecuencia`, `duracion`, `restricciones`, `requiere_receta` — separado de texto libre para poder calcular. **Objetivo final confirmado con el usuario: al elegir un artículo de Farmacia en una receta, sugerir la dosis calculada (peso más reciente de la mascota, ya existe en `pet_weights`, × `mg_kg`) que el veterinario puede editar antes de guardar.** 8 medicamentos reales ya cargados en `items` (department=Farmacia) el 24/07/2026 como datos de demo, con la dosis de referencia solo en `notes` (texto) — quedan pendientes de migrar a la tabla estructurada cuando se construya | Feature | Diferido a propósito — el usuario pidió solo dar de alta los artículos por ahora, sin construir el rediseño todavía |
| BL-031 | Mapa y Cobertura Espacial (`AX-MAPZN`) — entidad espacial genérica (dirección/punto) ligada muchos-a-muchos a personas/objetos/documentos | Feature | Sigue sin acotar (ver `docs/architecture/IDEAS_FUTURO.md`). La pantalla `AX-MAPZN` ya existe en versión mínima (ver BL-032 en Completados, 07/07/2026) con columnas directas simples — esta arquitectura genérica queda como evolución futura si el usuario decide que hace falta |
| BL-075 | **Redefinido tras conversación con el usuario el 31/07/2026** (ver BITACORA). Ya NO es "elegir rol por cita" — se descartó: la tarifa por operador no depende de su rol (nómina es por operador, no por "sombrero"), y "dos operadores distintos en un mismo trabajo" (ej. auxiliar + veterinario) ya se resuelve con **citas separadas** (una cita = un operador, ya soportado, sin cambios). Alcance final: **precio sugerido por costo-más-margen** — `precio = (suggested_duration_minutes / 60) × hourly_rate_del_operador × (1 + margen%)`, calculado en cuanto se elige operador + servicio al agendar (igual que el chequeo de disponibilidad), siempre editable al armar la orden (ya funciona así). Falta: (1) campo nuevo de margen de utilidad % — junto a `hourly_rate` en `OperatorCompensationProfile`; (2) decidir dónde se dispara el cálculo (vivo en el form, como `checkAvailability`, o al guardar); (3) fallback al `suggested_price` estático del catálogo cuando el operador no tiene perfil de compensación configurado (la mayoría hoy no lo tiene) | Feature | Sin construir — solo diseñado/acotado en conversación, no hay código todavía |

---

## Completados

| ID | Ítem | Sesión | Commit |
|---|---|---|---|
| — | Consolidación del rol de operador en un solo campo: existían 3 fuentes distintas para "qué tipo de operador es" (`operators.role` legado, `operators.operator_role_id` FK huérfana desde antes de los checkboxes, y `roles()` m2m vía `operator_role_assignments`, la real). `Api\OperatorController` ya mostraba un valor huérfano en la app móvil (ver fix anterior el mismo día). El usuario pidió consolidar en una sola fuente. Migración `2026_07_31_000000_consolidate_operator_role_fields` hace backfill del único operador que dependía del campo huérfano (sin fila m2m) y elimina las columnas `role`/`operator_role_id` de `operators`. `Operator::isVeterinario()` (gate real de `ClinicalVisitService::sign()`, exige veterinario con cédula para firmar una visita clínica) migrado de la FK huérfana a `activeRoles()`. Actualizados formulario/ficha/listado de operadores (quitado el campo "Rol operativo" de solo lectura y "Rol legado"), `Api\OperatorController`, `ClinicalVisitController`, y 3 tests que dependían de los campos legado. Respaldo completo (BD + código) antes del cambio, ver `backups/LISTA_RESPALDOS.md`. Verificado con test de Feature temporal (no quedó en el repo): `operators.index`/`show`/`edit` renderizan bien, guardar una edición real sigue asignando ambos roles correctamente. Suite completa sin regresiones (322 pasan, mismos 37 preexistentes) | 31/07/2026 | — |
| — | Corregido bug real de más de un mes: el respaldo automático diario de BD (cron 3am) fallaba en silencio — nombre de contenedor obsoleto (`backoffice-laravel-mysql-1` en vez de `estetican_mysql`) y log apuntando a `/var/log/` (sin permiso de escritura para el usuario del cron, la redirección fallaba antes de ejecutar una sola línea del script). Corregido en `scripts/auto_backup_db.sh` y el crontab (log ahora en `backups/estetican_backup.log`); probado manualmente, dump + subida a Google Drive exitosos | 31/07/2026 | — |
| — | Recibo de pago (`reports/invoice.blade.php`, backoffice web — pantalla `AgSpaSho`, botón "IMPRIMIR RECIBO"): mostraba $0 en citas cobradas 100% desde la app móvil (sin `Quote` de por medio — el flujo mobile de BL-074 hacia atrás nunca crea uno). `$acceptedQuote?->total_amount` sin fallback quedaba en `null`; ahora usa `$acceptedQuote?->total_amount ?? $booking->total_estimated_price`, igual que `_billing_summary.blade.php`. El listado de conceptos ahora cae a `$booking->services` cuando no hay `Quote`, y el historial de pagos suma también `Payment` (`payable_type=SpaBooking`, cobros vía `Api\PaymentController`), antes solo veía `CashLedger`/`BankLedger` legacy ligados a un Quote. Se agregó además una sección "NOTAS" que imprime `booking.notes` (de agendar) y todas las `booking_process_notes` (BL-074) — antes el recibo no imprimía ninguna nota. Verificado en vivo contra la cita real reportada por el usuario (`#27`, folio `R-000027`, sin Quote) vía `tinker` renderizando la vista directo: ahora sale $250.00/TOTAL LIQUIDADO y las 2 notas de proceso + la de creación. **De paso, mismo fix en `reports/work-order.blade.php`** ("orden de trabajo", el otro documento imprimible): sin `Quote` mostraba "No hay servicios aceptados registrados" pese a tener servicios reales — ahora cae a `$booking->services` igual que el recibo, y también ganó la sección de notas de proceso (antes solo imprimía `booking.notes`). Ambos verificados en vivo contra la cita `#27`. **Bug adicional real encontrado por el usuario:** el botón "Imprimir Documento" (`layouts/report.blade.php`, layout compartido por recibo/orden de trabajo/presupuesto) usaba `onclick="window.print()"` inline — la CSP del proyecto (`script-src` sin `unsafe-inline`, ver NT-042) lo bloqueaba en silencio, sin error visible; el clic no hacía nada. Movido a un `<script nonce="{{ csp_nonce() }}">` con `addEventListener`, mismo patrón ya usado en `agenda/global-create.blade.php`. **Tercer hallazgo, también reportado por el usuario:** en `MobCitaDet`/`MobCobro` las notas (de la cita y de proceso) no se podían editar en citas ya `completed`/`cancelled` — reutilizaban a propósito la misma regla que bloquea reprogramar/cambiar servicios de una cita cerrada, pero corregir una nota no reabre nada operativo. `Api\BookingController::update()` ahora permite un `PATCH` cuyo único campo sea `notes` incluso con la cita cerrada (cualquier otro campo, o mezclado con `notes`, sigue bloqueado); `Api\BookingProcessNoteController::update()` (no `store()`) ya no bloquea por estado. Frontend: se quitó el gate `editable` de los bloques de edición de notas en `MobCitaDet` (siempre editables), verificado en vivo con la cita `#27` (`completed`) vía `curl`. 4 tests nuevos (`InvoiceReportTest` ×2, `WorkOrderReportTest` ×2, incluye assert de que no hay `onclick=` inline) + 4 más de notas en cita cerrada (`BookingUpdateNotesTest`/`BookingProcessNoteTest`), suite completa sin regresiones (322 pasan, mismos 37 preexistentes) | 27/07/2026 | — |
| BL-074 | App móvil, notas de proceso: mientras una cita está "En proceso" (`MobCitaDet`), botón "Nota" junto al badge de estado abre un modal para que el operador registre observaciones del servicio en curso — varias notas cronológicas por cita, indexadas a la cita completa (no por servicio individual, a pedido del usuario). Tabla nueva `booking_process_notes` + `Api\BookingProcessNoteController` (`GET`/`POST /api/bookings/{id}/process-notes`, `PATCH .../process-notes/{note}`). En `MobCobro`, esas notas se listan y se pueden seguir editando/completando justo antes de cerrar el cobro. Corregidos dos bugs reales encontrados en la misma sesión: (1) `MobCobro` nunca mostraba la nota puesta al crear la cita en la agenda (`booking.notes` faltaba en su interfaz `BookingSummary` — el API sí la devolvía, pero el frontend no la leía); (2) esa misma nota original (`spa_bookings.notes`) no se podía editar ni durante el proceso ni al cobrar — ahora es editable en línea en ambas pantallas (clic → textarea → Guardar, sin pasar por el formulario completo de edición de la cita), y de paso se corrigió un bug de backend real en `Api\BookingController::update()`: un `array_filter` descartaba cualquier campo enviado explícitamente como `null` (incluyendo `notes`), así que limpiar una nota a vacío nunca se guardaba — reescrito para solo tocar los campos presentes en el payload (`sometimes` real), sin filtrar por valor. Las tres notas quedaron claramente etiquetadas: **Notas de la cita** (la de agendar, ahora editable en las dos pantallas) / **Notas del proceso** (las nuevas) / **Notas del cobro** (la del pago). 8 tests nuevos (`BookingProcessNoteTest` ×5, `BookingUpdateNotesTest` ×3), suite completa sin regresiones (315 pasan, mismos 37 preexistentes) | 27/07/2026 | — |
| BL-073 | Mascotas inactivas (`is_active=false`, BL-067): se encontraron 9 lugares del sistema que las mostraban sin filtrar (selectores de hotel/eventos de recurso/agendar SPA, listado clínico, ficha de cliente, mapa de zonas, recordatorios de recurrencia, KPI del dashboard, API móvil `index`). Nuevo scope `Pet::scopeVisible()` + `SystemSettings` → sección `pets`, campo `pets_show_inactive` (default `false`, apagado = ocultas en todo el sistema). A pedido explícito del usuario, **`pets/index` también quedó ligado a esta config** (no independiente como se planteó al inicio): con el switch apagado, elegir el filtro "Inactivas" da resultado vacío; encendido, vuelve a mostrarlas como antes. De paso se corrigió un bug real: el botón "Inactivar" del listado se mostraba también para mascotas ya inactivas — ahora se oculta cuando `pet.is_active` ya es `false`. Fetches de una mascota puntual por ID no se filtran (no tiene sentido ocultar una mascota ya referenciada). Verificado con suite completa: mismos 37 tests preexistentes fallando (no relacionados, ver BITACORA 26/07), sin regresiones nuevas | 27/07/2026 | — |
| — | App móvil, `MobCitaDet`: nuevo botón "Realizada" (visible en `scheduled` y `work_order`) para marcar una cita como llevada a cabo tal como se programó, sin la fricción del diálogo de tolerancia de horario que hoy solo aplica al botón "Iniciar servicio". Desde `scheduled` hace `PATCH status=work_order` en silencio y navega directo a cobro (`MobCobro`); desde `work_order` navega directo igual que "Completar y cobrar" (ambos botones conviven ahí a pedido explícito del usuario). Sin cambios de backend — el paso a `completed` nunca tuvo restricción de horario propia | 26/07/2026 | — |
| BL-072 | Bloqueo de pantalla del backoffice (terminal compartida, datos financieros/clínicos delicados). Botón manual "Bloquear pantalla" en el header + auto-bloqueo por inactividad — timeout **editable por cada operador** en `user/settings` (`users.screen_lock_idle_minutes`, nullable, cae al default global `config('backoffice.security.screen_lock_idle_minutes')` = 15 min). Desbloqueo solo con el password de quien bloqueó (regla nativa `current_password`, `throttle:5,1`); cualquier otra persona en el mismo equipo solo puede cerrar sesión. Estado "bloqueado ahora" vive en la sesión de Laravel (no en `users` ni tabla nueva) para sobrevivir a un refresh/otra pestaña — distinto del bloqueo 100% client-side de la app móvil (BL-038/BL-063), que no comparte sesión de navegador entre personas. Nuevo middleware `screen.lock`, `ScreenLockController`, vista standalone `auth/locked.blade.php`, JS de inactividad (`resources/js/modules/screen-lock.js`). 10 tests de Feature nuevos, todos verdes. De paso se detectaron 37 tests preexistentes fallando por no llamar `actingAs()` (deuda técnica sin relación con este ítem, ver BITACORA 26/07/2026) | 26/07/2026 | — |
| BL-070 | El selector de "Artículo" en Vacunas mostraba todos los artículos activos sin filtrar por categoría. Se reusó `items.department` (ya existente, documentado para esto — "Farmacia", "Accesorios", etc.) para filtrar: Vacunas ahora exige `department = 'Vacunas'`; nueva sección de Recetas gana selector opcional de artículos con `department = 'Farmacia'` (autocompleta fármaco/concentración vía Alpine.js, sin bloquear texto libre para compuestos/fuera de catálogo). `clinical_prescription_items` gana `item_id` nullable (FK → `items`, sin descuento de inventario todavía). A pedido del usuario, el campo "Departamento" del formulario de Artículos (`ArtEdi`/`ArtCre`) pasó de texto libre con sugerencias a un `<select>` cerrado (persiana) para eliminar errores de dedo que rompían el filtro en silencio | 24/07/2026 | — |
| BL-069 | Cerrar el flujo "Nueva mascota" (`pets/index.blade.php` → `clients.index?mode=pet_creation`): el `mode` nunca se leía ni tenía continuación. De paso se encontró y corrigió NT-043 (bug real y más grave: la búsqueda de clientes estaba rota en general, no solo en este flujo). `ClientController@index()` ahora expone `$petCreationMode`; en ese modo, cada cliente muestra "Agregar mascota aquí" en vez de Ver/Editar/Eliminar, que lleva a `clients.edit?open_pet_modal=1` y auto-abre el modal "Agregar Mascota" ya existente (reutiliza 100% la infraestructura de `client-form.js`, sin duplicar lógica) | 24/07/2026 | — |
| BL-068 | `onclick=`/`onsubmit=` inline bloqueados por la CSP (NT-042) en 8 vistas — algunos ejecutaban la acción **sin pedir confirmación** al estar el `onsubmit` bloqueado. Migrados todos al patrón `data-confirm` + `type="submit"` ya usado en `groups/`, `resources/`, `branches/`, `finances/`: `pets/partials/{index-blocks,index-table}.blade.php`, `pets/show.blade.php`, `user/show.blade.php`, `operators/partials/unavailabilities.blade.php`, `whatsapp/plantillas/index.blade.php`, `clinical/visits/show.blade.php`, `clinical/pets/show.blade.php` (4 casos) | 24/07/2026 | — |
| BL-067 | Mascotas: "Eliminar" ya no borra (ni soft-delete) — marca `is_active=false` para no perder historial (citas, pagos, expediente clínico). Detectado a partir de una queja de UX ("el botón de 4 no se lee", el ícono de basura no tenía `title`). Nueva columna `pets.is_active`; filtro del listado gana "Inactivas" (antes solo Todas/Activas/Fallecidas — el filtro de "Estado" en la vista de tabla tampoco tenía celda real, bug preexistente corregido de paso); badge combina Fallecida (`death_date`) > Inactiva (`is_active=false`) > Activa; checkbox "Mascota activa" en la ficha permite reactivar | 24/07/2026 | — |

| ID | Ítem | Sesión | Commit |
|---|---|---|---|
| BL-066 | Borrado de usuarios: `UserController::destroy()` hacía hard-delete sin validar dependencias. Nuevo `User::hasHistoricalDependencies()` chequea `operator_id`, causante en `activity_log`, y referencias en 13 tablas operativas/financieras (`audit_logs`, `operator_checkins`, `spa_bookings`, `cash_sessions`, `cash_movements`, `whatsapp_templates`, `booking_messages`, `recurrence_messages`, `resource_events`, `resource_event_updates`, `item_movements`, `documents`, `journal_entries`). Si tiene alguna, `destroy()` ahora marca `is_active=false`/`can_login=false` en vez de borrar; si no tiene ninguna, borra en duro como antes | 24/07/2026 | — |
| BL-047 (parcial) | PDF/impresión oficial del expediente clínico completo y de recetas individuales — primera dependencia de PDF del proyecto (`barryvdh/laravel-dompdf` v3.1.2, compatible con Laravel 13 sin ajustes). `layouts/pdf.blade.php` nuevo (CSS basado en tablas/bloques, no flex/grid — dompdf no los soporta de forma confiable, a diferencia de `layouts/report.blade.php` que ya usaba ambos). `ClinicalRecordPdfController@pet` descarga el expediente completo (identidad, alergias, condiciones, vacunas, peso, adjuntos por metadata, e historial de visitas con SOAP/diagnósticos/recetas completo); `@prescription` descarga una receta individual. Ambas rutas usan permiso `ver clinico` (mismo nivel que la ficha resumen ya existente) | 24/07/2026 | — |
| BL-047 (parcial) | Adjuntos clínicos reales: `ClinicalAttachmentController` (store/destroy) + `ClinicalAttachmentManager` (imagen optimizada sin recorte o PDF sin tocar bytes) + sección "Adjuntos clínicos" en `clinical/pets/show.blade.php`. Tabla/modelo ya existían desde BL-046 (Fase 1), solo faltaba la UI/upload real | 21/07/2026 | — |
| — | Fix subida de fotos: cropperjs v2→v1.6.2 | 25/05/2026 | `d1e4fdd` |
| — | Fix inicialización Cropper: shown.bs.modal + rAF | 25/05/2026 | `8c9a7e5` |
| — | Documentación técnica ITIL: NOTAS_TECNICAS, image-upload-system, ESTRATEGIA | 25/05/2026 | `0a593d4` |
| — | Push a GitHub + configuración SSH (clave `estetican-opi`) | 25/05/2026 | — |
| BL-005 | Cambiar password de `admin@localhost` desde la UI | 28/05/2026 | — |
| BL-006 | Bloquear endpoint `/up` con secret key en `bootstrap/app.php` | 14/06/2026 | — |
| BL-009 | Ecosistema Móvil: app `mob_apps/operador` conectada a API real | 13/06/2026 | — |
| BL-013 | Push a GitHub + deploy mov.estetican.org en producción | 15/06/2026 | `338690a` |
| BL-020 | Breadcrumbs universales en app móvil vía ScreenHeader | 16/06/2026 | `0d6a1a7` |
| BL-015 | App móvil: historial multi-modelo (SPA + Hotel) en MobPetJobs con filtros dinámicos | 15/06/2026 | `c9f1a2d` |
| BL-010 | App móvil: foto de mascota no se mostraba — URL relativa → `Storage::disk('public')->url()` | 14/06/2026 | — |
| BL-011 | App móvil: foto en PetSearch misma raíz que BL-010 — misma corrección | 14/06/2026 | — |
| BL-014 | `booking_grace_minutes` — verificado, funciona correctamente | 14/06/2026 | — |
| BL-017 | Módulo contable: 8 tablas, 8 modelos, 3 seeders, AccountingService, `account_id` en services | 14/06/2026 | — |
| BL-018 | Backoffice: pantallas Finanzas (accounts, payment_methods, document_series, cash_registers) | 14/06/2026 | — |
| BL-020 | App móvil: cobro con métodos dinámicos desde API + Payment model + asiento contable | 14/06/2026 | — |
| BL-019 | Backoffice: apertura/corte de caja + movimientos (retiro, depósito, gasto, pérdida, entrada) con póliza doble entrada automática | 15/06/2026 | — |
| BL-022 | App móvil: gestión de caja — ver sesión activa + registrar movimientos con póliza automática | 16/06/2026 | — |
| BL-007 | Cabeceras seguridad HTTP: X-Frame-Options, Referrer-Policy, Permissions-Policy en nginx de mob | 23/06/2026 | `1769893` |
| BL-021 | Comando `finanzas:migrar-ledgers-historicos` — JE para cash_ledgers y bank_ledgers históricos | 23/06/2026 | `d9097c0` |
| BL-023 | App móvil: Groomer→Operador + breadcrumb MobOpPkr + compresión imágenes + sync users↔operators | 30/06/2026 | `9c5c050` `58e92e4` |
| BL-024 | Backoffice: recordatorios WhatsApp Fase 1 — bandeja diaria con selección por checkbox, plantillas con variables, envío manual vía wa.me (sin automatización) | 01/07/2026 | `44084ae` `e754d27` |
| — | Backoffice: cambiar dueño de cualquier mascota (modal en `pets/show.blade.php`) | 02/07/2026 | `1e2713e` |
| BL-025 | Programar servicio (AgSpaCre, web y móvil): redondeo de hora a 5 min, fix de hora manual, horario operativo configurable, operador obligatorio + validación de traslape por operador | 02/07/2026 | `9161146` `3888b3c` |
| — | App móvil: fix `loadOccupied` no expandía slots ocupados por `duration_minutes` — permitía doble-agendar al mismo operador (ver NT-018) | 03/07/2026 | `71eae72` |
| BL-026 | Agenda Universal (web): vista Día/Semana/Mes estilo Google Calendar — toggle server-driven, grid semana (lunes-domingo) y mes (5-6 semanas, +3 chips y "+N más"), sin librería JS nueva | 03/07/2026 | `300148e` |
| BL-027 | Agenda móvil (Universal MobAgGbl y por operador MobOpAg): vista Día/Semana/Mes — `/api/agenda` acepta `view=day\|week\|month`. Semana/Mes corregidas a grid tipo Google Calendar (7 columnas/celdas con puntos de color) a pedido del usuario, reemplazando la lista agrupada inicial | 03/07/2026 | `c3cd3d0` `8fb7c99` `8682040` |
| — | App móvil: fix filtro por operador en Agenda (`MobAgGbl`/`MobOpAg`) — `/api/agenda` no incluía el operador asignado directamente (`operator_id`) cuando la cita no tenía presupuesto aceptado, y desaparecía del filtro (ver NT-021) | 07/07/2026 | `570f070` |
| BL-029 | Recordatorios de recurrencia: `services.recurrence_days` (ej. Baño cada 30 días) + pantalla WhatsApp > Recurrencias que calcula bajo demanda (sin cron) mascotas vencidas desde su última ejecución (`spa_bookings`/`spa_booking_services`, no `executed_services` — ver NT-020). Plantillas con `context` (cita/recurrencia) y variables propias. Preview del mensaje resuelto antes de abrir WhatsApp, compartido con Bandeja Diaria. Ampliado 07/07/2026: opción "Crear nueva plantilla" en el selector (modal, sin salir de la pantalla) en Bandeja y Recurrencias; botón "Previsualizar" con datos de ejemplo también en el modal y en el formulario completo de plantillas (crear/editar, `WspPlEdi`); "seleccionar todos" ya no incluye recordatorios ya enviados hoy (pero se pueden marcar a mano para reenviar) | 07/07/2026 | `570f070` |
| BL-030 | Calendario mensual en WhatsApp > Bandeja diaria (puntos de color por día: completadas / recordatorio pendiente / en riesgo de no asistir —incluye días pasados sin resolver—, checkboxes no excluyentes, clic en un día navega a esa fecha) + filtro "Estado" de Agenda Universal convertido de `<select>` único a checkboxes no excluyentes (mismo criterio: ninguno marcado = ver todos) | 07/07/2026 | `570f070` |
| BL-032 | Mapa y Cobertura Espacial — versión mínima (`AX-MAPZN`): Leaflet + OpenStreetMap (sin API key) en nueva pantalla dentro de "Operación". Muestra sucursales y direcciones de clientes (ya tenían lat/lng), permite ubicar mascotas y crear vehículos de reparto con clic en el mapa (`pets.lat/lng` nuevas, tabla `vehicles` nueva — mínima, sin placa/capacidad). Checkboxes no excluyentes por tipo. Deliberadamente **no** es la arquitectura genérica de BL-031, que sigue abierta | 07/07/2026 | `84c40b0` |
| — | Fix CSP bloqueaba silenciosamente las teselas de OpenStreetMap en `AX-MAPZN` (mapa en blanco) — de paso se corrigió que también bloqueaba la geocodificación (Nominatim) del editor de direcciones desde que existe la CSP (ver NT-022) | 07/07/2026 | `84c40b0` |
| BL-033 | App móvil: `MobTeam` ("Equipo") conectada a datos reales — reemplaza mockup hardcodeado. Endpoint `GET /api/team` (`Api\OperatorController::team()`) agrega por operador: check-in activo (real, vía `OperatorCheckin`/`User.operator_id`), pendientes/completadas del día (`SpaBooking`) y trabajo actual si tiene una orden abierta (`status = work_order`). Se eliminó el "Turno 08:00-16:00" inventado — no hay ese dato en el esquema — y el resumen operativo ahora agrega datos reales del equipo en vez de números fijos | 10/07/2026 | `fdeb7b6` |
| — | Fix schema drift: `users.is_operator`/`operator_code` existían en producción sin migración commiteada, rompía la base `testing` (ver NT-023) — migración idempotente nueva, no-op confirmado en producción | 10/07/2026 | `fdeb7b6` |
| — | App móvil: barra de navegación reordenada a pedido del usuario (Agenda, Mascotas, Clientes, Operador) — se quitaron "Equipo" y "Directorio" del menú principal/tabs por ser redundantes con "Operador" y con las pantallas de Mascotas/Clientes ya existentes; las pantallas en sí no se borraron, solo dejaron de estar enlazadas | 10/07/2026 | `8322bea` |
| BL-034 | App móvil: `MobUserConfig` conectado a cuenta real — el usuario reportó que no permitía configurar nada real. Agregado: editar nombre/apellido/correo, cambio de contraseña (valida contraseña actual), foto de perfil (subir/quitar, reutiliza `UserPhotoImageManager`), selector de tema Claro/Oscuro/Sistema (paleta oscura nueva completa vía variables CSS, aplica a toda la app de inmediato, persiste en `localStorage`). Endpoints nuevos `PATCH /api/me`, `PUT /api/me/password`, `POST`/`DELETE /api/me/photo`. **GMT/zona horaria por usuario quedó deliberadamente fuera de esta pasada** — los timestamps de `spa_bookings` no tienen ancla de zona horaria (son naive), así que una conversión por-usuario mostraría horas incorrectas; requiere primero decidir una estrategia de zona horaria a nivel de datos, no es un simple selector de UI | 10/07/2026 | `8322bea` |
| BL-035 | App móvil: editor de foto antes de subir (recorte + rotar + marca de agua opcional) para la foto de perfil de `MobUserConfig`, vía `cropperjs` `1.6.2` (misma versión fijada que usa el backoffice web, NT-001) nuevo en `mob_apps/operador`. La marca de agua (nombre + fecha, pequeña y legible, franja inferior semitransparente) es **una regla de negocio, no una preferencia personal**: se activa/desactiva desde el Backoffice web (`Configuración del sistema` → sección nueva "Fotografías", `photo_watermark_enabled`, default apagado), expuesta a la app móvil vía `GET /api/settings/photos`. La fecha usa el metadato EXIF `DateTimeOriginal` de la foto si existe (parser propio, sin dependencia nueva, `src/lib/exifDate.ts`); si no hay EXIF, usa la fecha de subida | 10/07/2026 | `8322bea` |
| BL-036 | App móvil: fix — mascotas existentes no permitían cambiar la foto ("se gestiona desde el backoffice", mensaje hardcodeado sin excepción). Ahora usa el mismo `PhotoEditorModal` (recorte/rotar/marca de agua) de BL-035, conectado a nuevos endpoints `POST`/`DELETE /api/pets/{pet}/photo`. De paso se encontró y corrigió que la subida de foto de mascota vía API (tanto al crear como — ahora — al editar) **no comprimía la imagen en absoluto** (guardaba el archivo crudo) y no dejaba registro en la galería (`pet_photos`) como sí hace el backoffice web — ahora usa `PetPhotoImageManager` igual que la ruta web, con el mismo registro de trazabilidad (`photo_type: perfil`, `is_primary`, fecha EXIF vía `extractTakenAt()`) | 10/07/2026 | `8322bea` |
| — | Fix `ScreenHeader.tsx`: el tag de depuración de pantalla (ej. `MobPetDet`) desaparecía por completo cuando la pantalla mostraba breadcrumbs — el branch de breadcrumbs nunca lo renderizaba, a diferencia del branch sin breadcrumbs. Afecta a **toda la app**, no solo mascotas; reportado por el usuario como "no muestra el nombre de la pantalla" en Mascotas, que es donde más se nota porque casi siempre se llega ahí con breadcrumbs | 10/07/2026 | `8322bea` |
| BL-037 | App móvil: auditoría de código pedida por el usuario ("busca software redundante, que no se use o que no esté resuelto"). Sin herramienta de navegador en este entorno, se corrieron todos los chequeos estáticos disponibles (`tsc`, `npm run build`, `npm audit`) más una revisión manual exhaustiva. Encontrado y corregido: (1) 7 dependencias npm sin ninguna referencia en el código (`@google/genai`, `clsx`, `dotenv`, `express`, `lucide-react`, `motion`, `tailwind-merge`) — desinstaladas; (2) links rotos en `Directory.tsx`/`AssignService.tsx` que apuntaban a rutas inexistentes (`/admin/assign`, `/admin/directory`, `/agenda-global`) — corregidos a las rutas reales (`/directorio`, `/directorio/asignar`, `/agenda`); `AssignService.tsx` además le faltaba el import de `Link` (error de compilación preexistente, no detectado antes); (3) el formulario de alta de mascota (`NewPetForm` dentro de `PetDetail.tsx`) era la única pantalla que había quedado con el selector de foto viejo (sin recorte/rotar/marca de agua) tras BL-035/BL-036 — ahora usa el mismo `PhotoEditorModal`, las 3 pantallas de foto quedan consistentes. **Dejado sin tocar a pedido del usuario:** 4 archivos huérfanos sin ninguna ruta ni import (`ActiveService.tsx`, `GroomerDashboard.tsx`, `client/Booking.tsx`, `client/Dashboard.tsx`, ~730 líneas) — parecen resto de un template sin terminar (`package.json` se llama literalmente `"react-example"`); quedan documentados acá por si se decide borrarlos o retomarlos más adelante | 10/07/2026 | `8322bea` |
| — | Fix test flaky por hora del día en `TeamPanelTest` (ver NT-024) — no relacionado a código de producción | 10/07/2026 | `8322bea` |
| BL-038 | App móvil: bloqueo de sesión — timeout por inactividad (5 min) + bloqueo automático al cambiar de app (`visibilitychange`) + botón manual "Bloquear ahora" en el menú, todo sin cerrar sesión (el token sigue válido, solo se tapa la UI). Desbloqueo con contraseña (nuevo endpoint `POST /api/me/verify-password`, no cambia la contraseña ni el token) **o** con Face ID/huella/PIN del teléfono vía WebAuthn — verificación 100% local en el dispositivo, sin involucrar al servidor (el "challenge" es aleatorio generado en el celular, no hay tabla de credenciales en el backend); si el navegador no soporta WebAuthn o el usuario no lo activó, cae automáticamente a contraseña. Activación opcional en `MobUserConfig` → sección "Seguridad". **No se pudo probar de punta a punta en este entorno** — WebAuthn requiere hardware biométrico real de un navegador/teléfono, no hay forma de simularlo acá | 10/07/2026 | `8322bea` |
| — | Fix `LockScreen`: campo de contraseña y botón "Desbloquear" se veían diminutos (más chicos que el texto "Cerrar Sesión") — causa raíz: `max-w-xs`/`max-w-sm` colisionaban con tokens custom `--spacing-xs`/`--spacing-sm` del tema Tailwind, resolviendo a 4px/8px en vez de ~320px/384px (ver NT-026). Confirmado por el usuario probando en producción real tras el fix | 12/07/2026 | `8322bea` |
| — | Fix crítico de seguridad — el candado de sesión (BL-038) se perdía por completo en cualquier reload o "atrás" del navegador, sin importar si estaba bloqueado manual o automáticamente. Dos causas encontradas y corregidas en `AppLockContext.tsx`: (1) el estado persistido en `localStorage` se borraba antes de tiempo por una carrera con la carga asíncrona de `useAuth()` (ver NT-027); (2) escribir la contraseña en `LockScreen` sin terminar de desbloquear marcaba incorrectamente la sesión como desbloqueada en lo persistido (ver NT-028). Confirmado por el usuario en producción real tras el fix | 12/07/2026 | `d09b1f0` |
| — | App móvil: panel de "citas pendientes sin resolver" en `MobAgGbl` (Agenda) — antes ocupaba mucho espacio siempre expandido arriba de la lista; a pedido del usuario ahora es un botón chico con colores de alarma junto a Día/Semana/Mes que abre una ventana (hoja deslizable desde abajo, mismo patrón que el menú lateral) con el detalle | 12/07/2026 | `d09b1f0` |
| BL-039 | Backend: nueva columna `spa_bookings.created_by_user_id` (FK nullable a `users`, `nullOnDelete`) — registra qué usuario (dueño de la sesión, web o móvil) agendó cada cita. A pedido del usuario, que preguntó si esto ya se guardaba y no era el caso (antes solo se guardaba `operator_id`, el operador asignado/que atiende, no quién la registró). Capturado vía `auth()->id()` en `BookingService::scheduleSpaSession()` (ruta web) y `Api\BookingController::store()` (ruta móvil) — los dos únicos caminos de creación de citas SPA hoy. Relación `SpaBooking::createdBy()`. 2 tests nuevos (uno por ruta) confirman el guardado | 12/07/2026 | `d09b1f0` |
| BL-040 | Backoffice: envío de plantillas también por correo (paso 1 de la hoja de ruta de mensajería — ver comparación con MoeGo). Sección "Servicio de Correo" completa en Configuración del sistema (usuario, contraseña cifrada, encriptación, remitente), bridgeada de verdad a `config('mail.*')`. Campo `subject` nuevo en `whatsapp_templates`. Nuevo `TemplateMessageMail` con botón "Escríbenos por WhatsApp". Bandeja Diaria y Recurrencias ahora tienen selector de canal (WhatsApp/Correo) reusando la misma cola de envío y previsualización; `booking_messages`/`recurrence_messages` ganan columna `channel`. De paso se corrigió un bug preexistente real: `ServiceSummaryMail` llamaba a un método `SystemSettings::get()` que no existe — hubiera tronado (fatal) la primera vez que alguien activara el resumen automático por correo. 9 tests nuevos | 12/07/2026 | `69a03fc` |
| — | Fix `system-settings/smtp-test` daba 404, y el campo de encriptación SMTP ofrecía valores (`ssl`/`tls`) que Symfony Mailer no reconoce (son `smtp`/`smtps`) — encontrado por el usuario probando en producción real tras BL-040. Ver NT-030 (con 3 adendas del mismo día: opciones corregidas, resguardo genérico para valores `select` obsoletos guardados en BD, y la gotcha de que `tinker`/`artisan` no pasan por el middleware que aplica `SystemSettings` a la config de mail) | 12/07/2026 | `69a03fc` |
| BL-042 | Backoffice: asistente de IA (Claude) para el widget de chat del sitio público de WordPress — informativo, sin conexión a CRM/agenda. Responde preguntas sobre el catálogo de servicios activo (`ServiceCatalogService::getActiveServices()`, nuevo) y siempre invita a un botón fijo de CTA configurable. Sección nueva `ai_assistant` en Configuración del sistema (API key cifrada, modelo, prompt extra, texto/URL del botón, token del widget, origen CORS permitido). Nuevo `Api\AssistantChatController` (`POST /api/assistant/chat`, `GET /api/assistant/config`), rutas públicas fuera de `ApiAuthenticate`, protegidas con `VerifyAssistantSiteToken` + `HandleAssistantCors` (middleware global a medida, no `config/cors.php` — ver NOTAS_TECNICAS) + `throttle:15,1`. Tope de 30 mensajes por sesión anónima (`service_ai_chats`/`service_ai_messages`, sin relación a `clients`/`leads`). Primera integración HTTP saliente y primer uso de LLM en el proyecto. **Decisiones explícitas del usuario que acotaron el alcance:** Facebook/Instagram quedan fuera — son solo informativos y se resuelven con las respuestas automáticas nativas de Meta Business Suite (sin código); la comunicación real conectada al CRM (citas, estado de mascotas) le corresponde a la futura app de clientes (BL-012), no a este chatbot. Fase 2 pendiente: widget JS embebible + snippet de inserción en WordPress. 9 tests nuevos | 13/07/2026 | `27d1440` |
| BL-042b | Fase 2 del asistente de IA (BL-042): widget embebido en el sitio real de WordPress (`estetican.org`, página de Servicios) — botón flotante, panel de chat, botón de CTA a WhatsApp. Confirmado funcionando end-to-end contra Anthropic real. **Bug real encontrado y corregido en el camino:** el editor de WordPress usado le aplica `wpautop` (filtro de párrafos) al contenido de los campos HTML/CSS/JS *antes* de envolverlos en sus etiquetas — cualquier JavaScript con texto inline terminaba con `<p>`/`</p>` insertados adentro, rompiéndolo (ver NT-032). Solución final: toda la lógica vive en un archivo externo (`public/assistant-widget-wp.js`), cargado con una sola etiqueta `<script src="..." data-api-base="..." data-token="..." async>` **sin texto en el cuerpo** — inmune a wpautop porque no hay contenido que corromper. Contenido versionado en `wp-portal/` (nuevo directorio, no se despliega, solo repositorio de trabajo para copiar/pegar al editor de WP) | 14/07/2026 | `a7ad910` |
| BL-043 | Backoffice + móvil: advertencia (no bloqueo) de cobertura geográfica al agendar citas — si la mascota queda a más de `coverage_radius_km` (Configuración del sistema → Cobertura Geográfica, default 15 km) de la sucursal activa más cercana, se muestra un aviso al staff (toast amarillo en web, banner en la app móvil) sin impedir guardar la cita. Usa coordenadas ya existentes (`pets.lat/lng`, con fallback a `addresses.lat/lng` del cliente, contra `branches.lat/lng`) — sin tabla nueva. Nuevo `App\Support\Geo\CoverageChecker`/`DistanceCalculator` (Haversine), conectado en los dos puntos de creación de citas SPA (web y móvil). A pedido del usuario, para evitar agendar clientes fuera de zona mientras se decide la cobertura real del negocio (ver restricción de BL-012). 9 tests nuevos | 14/07/2026 | `bc36006` |
| BL-041 | Backoffice: preferencias de comunicación del cliente (opt-out por categoría — ofertas, recordatorios de servicio, estado de trabajo/resúmenes, estado de cuenta, otros), a pedido del usuario tras BL-040, con motivo legal (LFPDPPP, derecho de oposición sobre mercadotecnia). Las 5 categorías arrancan opt-out por defecto (opted-in), decisión explícita del usuario. Solo (b) recordatorios y (c) estado de trabajo tienen emisor real hoy — se bloquea el envío en servidor (422), no solo en la UI, en `BookingMessageController`/`RecurrenceMessageController` (ambos canales) y en `ServiceSummaryMail`. Nueva sección en la ficha de cliente (staff-managed) + autogestión pública sin login vía enlace firmado en el pie de los correos (`URL::temporarySignedRoute`, primer uso de rutas firmadas en el proyecto, válido 1 año) — `ClientPreferencesController` nuevo, fuera del grupo `auth` de `routes/web.php`. (a) ofertas y (d) estado de cuenta quedan solo con la preferencia guardada — no existe ningún emisor de campañas ni de estado de cuenta todavía. 7 tests nuevos | 12/07/2026 | `69a03fc` |
| BL-044 | Atomizar apellido de clientes: columnas nuevas `apellido_paterno`/`apellido_materno` en `clients` (paso 1 de una atomización más amplia clients→users→operators, a pedido del usuario). `last_name` queda vestigial — accessor calculado en `Client`, mantiene compatibilidad con todo el código de solo lectura. Backfill idempotente (`clientes:migrar-apellidos --dry-run`) con heurística por conteo de palabras + reporte CSV de casos ambiguos (ninguno en los 9 clientes reales de producción). Actualizados 8 controllers (select/where/orderBy/validación/eager-load de columnas restringidas), 2 vistas Blade (alta/edición web) y el formulario de cliente en la app móvil (`ClientDetail.tsx`). Respaldo completo previo (dump BD + tarball del proyecto + tag git `pre-bl044-apellidos`) por tratarse de producción real (bind mount directo, sin capa de staging). Suite completa sin regresiones (37 fallidas preexistentes, 119 pasan — igual que antes) | 14/07/2026 | `255d271` |
| BL-046 | Expediente clínico veterinario (formato SOAP) — módulo nuevo e **independiente** del spa/grooming/hotel actual, activable/desactivable (`SystemSettings` → `clinical.clinical_module_enabled`, default apagado; sin nada en nav/rutas mientras esté así). Comparte identidad atómica con `clients`/`pets` (nunca duplica) y el pool de `operators` (rol nuevo `veterinario`, con `operator_role` + permisos Spatie `clinico.*`/`alergias.administrar`). 7 tablas nuevas (`clinical_visits` como encabezado SOAP central, `pet_weights`, `pet_allergies`, `pet_conditions`, `clinical_diagnoses`, `clinical_prescriptions`/`items`, `clinical_attachments`) + `pet_vaccinations` alterada y por fin conectada (dejó de estar huérfana). Firma de visita con cédula profesional (`Operator::professional_license`, ya existía sin uso) deja la nota inmutable — corrección solo vía nota aclaratoria enlazada (`amends_visit_id`), nunca edición in-place (guard en `ClinicalVisit`). Soporta atención de veterinario externo (`is_external`) sin exigir firma, más una ficha/carpeta imprimible para entregar al externo. `VaccinationEligibilityChecker` (mismo patrón que `CoverageChecker` de BL-043) advierte —nunca bloquea— vacunas core vencidas al agendar spa/hotel, y queda inerte si el módulo está apagado. **2 bugs reales encontrados y corregidos en pruebas contra producción real (transacción revertida, sin dejar datos falsos):** `sign()` no persistía nada por campos faltantes en `$fillable` (NT-033); una bandera de bypass de guard quedaba pegada en `true` tras usarse (NT-034). Controllers en `app/Http/Controllers/Clinical/`, vistas en `resources/views/clinical/`, dominio en `App\Domain\Clinical`. Respaldo completo previo (dump BD + tarball + git tag). Suite sin regresiones (37 fallidas preexistentes, 132 pasan = 119 + 13 nuevas). Fase 2 diferida a propósito → BL-047 | 14/07/2026 | `255d271` |
| BL-048 | Vacunas como servicio del catálogo único (reemplaza el diseño inicial de "catálogo de vacunas aparte" tras corrección del usuario): en vez de una tabla `vaccine_catalog` nueva, cada vacuna es un `Service` más (`type='vaccine'`, con `recurrence_days` y `is_core_vaccine` nuevo) — el usuario señaló que el catálogo de servicios debe ser uno solo (spa, hotel, recogida a domicilio, vacunas, lo que se agregue después), no catálogos separados. `pet_vaccinations` gana `service_id` (FK); `vaccine_name` pasa a ser un snapshot automático del servicio elegido (elimina el error de dedo sin quitar la columna). Cero tablas/pantallas nuevas para recordatorios — se reusa 100% `RecurrenceMessageController`/`recurrence_messages`/plantillas ya existentes (BL-024/029), con una sola rama nueva: si `service.type==='vaccine'`, la "última vez" sale de `pet_vaccinations.applied_at` en vez de `spa_bookings`. `VaccinationEligibilityChecker` (BL-046) cambia su fuente de vacunas "core" del texto libre `clinical_core_vaccines` (eliminado de `SystemSettings`) a `services.is_core_vaccine`. **Modelo operativo confirmado con el usuario:** una vacuna aplicada vía Veterinaria se refleja informativamente en Spa/Hotel (nueva sección de solo lectura en `pets/show.blade.php`, sin mostrar quién la aplicó) — el detalle completo con operador/lote/vía sigue exclusivo de `clinical/*`. Existencias/stock por sucursal quedan explícitamente fuera de alcance (ver BL-049). 6 tests nuevos/actualizados, suite sin regresiones (37 fallidas preexistentes, 136 pasan) | 14/07/2026 | `255d271` |
| BL-050 | Maestro de artículos (`items`) — fundación atómica para el futuro módulo de inventario (BL-049), a pedido explícito del usuario ("para no tener que deshacer y rehacer"): tabla nueva sin ninguna columna de existencias/stock, solo identidad de producto (`name`, `department` con datalist de sugerencias, `brand`, `presentation`, `is_active`, `notes`). Mismo campo `department` (texto libre) agregado también a `services`, mismo espíritu de agrupación. CRUD mínimo (`ItemController::store/update/destroy`, sin index/listing propio — alta rápida desde un mini-formulario inline en la pantalla de vacunas) protegido con el permiso `alergias.administrar` ya existente. **Corregido en la misma sesión:** el CRUD se movió a pantalla propia (Catálogos → Artículos, `index`/`create`/`edit` nuevos) con permiso dedicado `catalogo_articulos`, independiente del módulo clínico — el usuario señaló que no correspondía que el maestro de artículos viviera dentro de Veterinaria (ver NT-035 sobre el `middlewareFor()` usado para el permiso por acción). `pet_vaccinations` gana dos columnas independientes: `item_id` (FK nullable → `items`, identifica la marca/presentación específica usada, autocompleta `manufacturer` desde `item.brand`) e `is_external` (bandera propia, **distinta** de `clinical_visits.is_external` — registra que la vacuna se aplicó fuera de EstetiCAN, sin descontar inventario ni cobrar al cliente, solo para efectos de protección/elegibilidad). `VaccinationEligibilityChecker` confirmado: una vacuna externa válida sí cuenta como protección (el objetivo es "¿está protegida la mascota?", no "¿se cobró aquí?"). **Bug real encontrado:** directivas Blade adyacentes sin espacio (`@endif@if(`) producen `unexpected token "endif"` — corregido insertando espacio. 4 tests nuevos, suite sin regresiones (37 fallidas preexistentes, 140 pasan) | 14/07/2026 | `255d271` |
| — | Corrección de BL-050 en la misma sesión: CRUD de `items` movido fuera del módulo clínico a pantalla propia **Catálogos → Artículos** (`app/Http/Controllers/ItemController.php`, `index`/`create`/`edit` nuevos + vistas). Permiso dedicado `catalogo_articulos` (ver/crear/editar/eliminar, mismo patrón que `catalogo_servicios`) en vez de reusar `alergias.administrar`; rol `veterinario` conserva `ver`+`crear` para el alta rápida inline desde vacunas, `eliminar` queda solo para admin. De paso, activado `clinical_module_enabled` en producción real a pedido del usuario y corregido un caso real de la trampa #3 de BL-044 sin cobertura hasta ahora (`ResourceEventCrudTest` creaba el cliente con `last_name` descartado en silencio). 6 tests nuevos (`ItemCrudTest`). Ver NT-035 sobre el gotcha de `Route::resource()->middlewareFor()`. Suite sin regresiones (37 fallidas preexistentes, 143 pasan) | 14/07/2026 | `255d271` |
| BL-045 | Atomizar apellido de `users` (fase 2, continuación de BL-044) — mismo patrón: columnas nuevas `apellido_paterno`/`apellido_materno`, `last_name` vestigial vía accessor calculado en `User`. Backfill idempotente (`usuarios:migrar-apellidos --dry-run`), 4 usuarios reales migrados sin ambiguos. A diferencia de `clients`, casi sin impacto a nivel de query (`UserController::index()` es `User::all()`, sin `select`/`orderBy` restringido) — el foco real estuvo en los 3 formularios (`user/create`, `user/edit`, `user/settings`) y el endpoint móvil `PATCH /api/me` (`Api\ProfileController`, usado por `MobUserConfig`), que sí escribía el campo directamente. `toApiArray()` sigue exponiendo `last_name` calculado para no romper compatibilidad con la app móvil. Auditoría de tests: 25 archivos creaban un usuario de prueba con `last_name` como boilerplate (reemplazo mecánico a `apellido_paterno`); `Api/ProfileTest.php` sí ejercita el campo de verdad vía API y se actualizó para probar los 2 campos nuevos. Respaldo previo (dump BD + tag git `pre-bl045-apellidos-users`). Suite sin regresiones (37 fallidas preexistentes, 143 pasan, mismo conteo que antes por no agregar tests nuevos). BL-045b (operators, fase 3) queda pendiente en Activos | 14/07/2026 | `a766165` |
| BL-051 | Banderas de visibilidad para el asistente IA (BL-042) en `services` e `items`, a pedido del usuario ("marca para ver si la IA los puede mencionar o no"). 3 campos nuevos en `services` (`ai_visible`, `is_generic`, `is_emergency`, todos default `false`) y 3 en `items` (`ai_visible` default `false`, `stock_quantity` unsigned int default `0`, `price` decimal nullable). `ServiceCatalogPromptBuilder` reescrito: nuevo método de dominio `getAssistantVisibleServices()` (no se tocó `getActiveServices()`, usado en otros lugares) filtra servicios por `is_active`+`ai_visible`; nueva sección de artículos en el prompt, consultando `Item` directo (sin capa de dominio, igual que `ItemController`) filtrando `is_active`+`ai_visible`+`stock_quantity > 0`. Servicios `is_generic` no muestran precio (el asistente ofrece agendar cita de evaluación en su lugar — pensado para "Cirugía" y similares sin costo fijo); `is_emergency` instruye al asistente a invitar de inmediato al botón de WhatsApp ya existente (`ai_assistant_cta_url`), sin construir un botón ni mecanismo de envío nuevo. **Decisión explícita del usuario:** todos los flags nuevos arrancan en `false`/`0` — nada cambia de comportamiento hasta marcarse a mano, y los artículos reales no entran al catálogo IA hasta capturar existencia. Formularios de Servicios y Artículos actualizados con los checkboxes/campos nuevos (`price`, `stock_quantity` incluidos en Artículos). Sin tests nuevos (cambio de configuración/datos, sin lógica de negocio nueva que testear); suite completa sin regresiones (37 fallidas preexistentes, 143 pasan). **Fuera de alcance a propósito** (ver BL-052 y BL-049): columnas estructuradas de especie/talla/sexo para búsqueda de artículos (se decidió confiar en que la IA infiera de texto libre `name`/`department`/`brand`/`presentation`/`notes`), fotos de artículos, automatización real de publicación en catálogo de WhatsApp/redes, y stock transaccional real (movimientos/almacenes) | 15/07/2026 | `d606f7f` |
| — | Reorganización de navegación: "Catálogos" se había vuelto cajón de sastre (Servicios+Artículos+Operadores+Tipos de operador+Sucursales+Usuarios+Bitácora). A propuesta del usuario ("es momento de separar los módulos"), se crearon 2 grupos nuevos (`app/Support/Navigation/Groups/{InventoryNavigation,HrNavigation}.php`, registrados en `MainNavigation::structure()`): **Inventario** (Artículos — catálogo de venta, BL-050/051) y **RH** (Operadores, Tipos de operador, Usuarios). Sucursales y Bitácora de actividad se quedaron en Catálogos (no son personal ni inventario). Finanzas ya existía como módulo propio, se dejó sin renombrar. Cambio puramente de navegación — mismos permisos Spatie, mismas rutas, cero cambios de esquema/datos. Suite sin regresiones (37 fallidas preexistentes, 143 pasan) | 15/07/2026 | — |
| — | Navegación anidada: persiana única **"Administración"** agrupa Inventario, RH, Finanzas y Veterinaria como sub-secciones de un solo dropdown en el Backoffice web (la app móvil no se toca) — a pedido del usuario, para no saturar la barra superior con módulos de uso menos frecuente que Agenda/Operación. `MainNavigation` gana una segunda forma de grupo (`subgroups`, solo para Administración) además de la plana (`items`) que ya usan Operación/Catálogos/WhatsApp; `groups()`/`mobileLinks()` manejan ambas. Nuevo componente `<x-main-navigation-item>` (evita duplicar el markup de un item en el render plano vs. anidado). Se corrigió `ClinicalModuleToggleTest`, que verificaba "Veterinaria" como grupo de nivel superior — al anidarla, el assert habría pasado siempre por accidente sin detectar regresiones reales; ahora usa un helper que aplana subgrupos. **Ajuste el mismo día:** Clientes se probó dentro de Administración primero, pero el usuario pidió que quedara en su propia persiana de nivel superior (junto con Sucursales, que se movió ahí desde Catálogos) por ser de uso más frecuente — ver fila siguiente. Suite sin regresiones (37 fallidas preexistentes, 143 pasan) | 15/07/2026 | — |
| — | Ajuste: **Clientes** (Abrir clientes, Mascotas, y ahora también Sucursales) se queda como persiana propia de nivel superior, fuera de "Administración" — corrección sobre la fila anterior, a pedido del usuario. Sucursales se movió de Catálogos a Clientes (mismo permiso `ver sucursales`). Catálogos queda solo con Servicios + Bitácora de actividad; Administración queda con 4 sub-secciones (sin Clientes). Suite sin regresiones (37 fallidas preexistentes, 143 pasan) | 15/07/2026 | — |
| BL-045b | Atomizar apellido de `operators` (fase 3, cierra la iniciativa clients→users→operators). A diferencia de `clients`/`users`, `operators` no tenía **ningún** campo de nombre separado — solo `name` (legado, a veces espeja el login del `User` vinculado, a veces duplica el nombre completo) y `full_name` (string único mezclando nombre(s) + apellidos, ej. "Tomas Alejandro Martinez Gutierrez"). A pedido del usuario se agregaron los 3 campos completos (`first_name`, `apellido_paterno`, `apellido_materno`), no solo los 2 apellidos, para quedar consistente con `clients`/`users` — `full_name` pasa a ser accessor calculado. Backfill con heurística de 3 vías (`operadores:migrar-apellidos --dry-run`): últimas 2 palabras = apellidos, el resto = nombre(s); ambiguo solo si son 5+ palabras (nada en los 2 operadores reales). **Trampa nueva no vista en BL-044/045 (ver NT-036):** `operators.full_name` era `NOT NULL` sin default en la BD real (a diferencia de `last_name` en `clients`/`users`, que sí nacieron nullable) — romper esa restricción tronaba cualquier alta nueva de operador en cuanto se quitó `full_name` de `$fillable`; se agregó una migración extra para volverla nullable. Auditoría completa de `OperatorController` (búsqueda, 4 `orderByRaw`, `duplicate()`/`buildDuplicateName()`) y de 5 eager-loads con columnas restringidas que aún pedían `full_name` (`SpaBookingController`, `Api\BookingController`, `Api\AgendaController` ×2, `Api\OperatorController` ×2) — de no corregirse, el nombre se habría visto vacío en agenda/API móvil sin ningún error. `UserController::syncOperatorRecord()` simplificado: ya no concatena un string, pasa `first_name`/`apellido_paterno`/`apellido_materno` directo del `User` atomizado al `Operator` vinculado. 2 tests exigían el campo de verdad vía HTTP (`OperatorBranchSelectionTest`, `OperatorPhotoUploadTest`), actualizados con cuidado; 9 archivos con `Operator::create(['full_name' => ...])` como boilerplate, reemplazo mecánico. Respaldo previo (dump BD + tag git `pre-bl045b-apellidos-operators`). Suite sin regresiones (37 fallidas preexistentes, 143 pasan). Cierra la iniciativa de atomización de apellidos iniciada en BL-044 | 14/07/2026 | `9dbd705` |
| BL-054 | "IM sencillo" (BL-049, fase mínima) — ledger append-only `item_movements` (tipo entrada/salida/consumo_servicio/ajuste/pérdida, delta con signo, `branch_id` nullable ya presente para el futuro multi-sucursal, `reference` polimórfico), mismo patrón sin-saldo-cacheado que `CashLedger`/`BankLedger`. `items.stock_quantity` pasa de editable a mano a caché mantenido por `ItemMovementService::record()` (`DB::transaction`+`lockForUpdate()`, único ejemplo de locking real que ya existía en el repo, en `AccountingService::getNextFolio()`) — tuvo que volverse columna con signo (antes `unsigned`) porque un consumo sin entrada previa puede dejarla negativa, y eso es información real, no un error. Nueva sección "Movimientos de inventario" en Artículos (registrar entrada/ajuste/pérdida + histórico, permiso reutilizado `editar catalogo_articulos`). Conectado el primer consumo automático real: aplicar una vacuna con artículo ligado (no externa) genera un movimiento `consumo_servicio` de -1 (`PetVaccinationController::store()`). 14 tests nuevos, suite sin regresiones (37 fallidas preexistentes, 151 pasan) | 15/07/2026 | `1251015` |
| BL-055 | "Grupos" — combos de Servicios + Artículos con cantidad, un clic los agrega todos a una cotización, facturados desglosados (ej. "Corte de cola de perro" = 0.5 hrs Veterinario + 5 vendas). Planeado con `EnterPlanMode` dado el tamaño (toca `Quote`/`QuoteItem`, `AccountingService`, billing) — plan completo en la sesión del 15/07/2026. Tablas nuevas `groups`/`group_components` (FKs duales `service_id`/`item_id` con CHECK "exactamente uno", no morphs — dominio cerrado a solo 2 tipos, confirmado con el usuario; `restrictOnDelete` en vez de `nullOnDelete` para no romper el CHECK); `quote_items` gana `item_id`/`quantity`/`group_id` (`service_id` se vuelve nullable); `spa_booking_services` gana `quantity`/`group_id`; tabla nueva `spa_booking_items` paralela. Precio del Grupo **no se cachea** — `Group::calculatedPrice()` usa `SUM()` al vuelo (mismo patrón que `Account::balance()`), así un cambio de precio en el catálogo se refleja sin invalidar nada. **Bug crítico real encontrado y corregido en el mismo cambio** (no como seguimiento): `AccountingService::buildDebitLines()`/`buildDebitLinesFromBooking()` asumían `$item->service` sin guardar — con una sola línea de Artículo en una cotización aceptada, el cobro truena con error fatal o (peor) prorratea el ingreso silenciosamente entre cuentas de servicios sin ningún error visible; `items` ganó `account_id` (mismo patrón que `services.account_id`) para poder clasificar el ingreso. `_quote_manager.blade.php` gana "Agregar grupo completo"/"Agregar artículo suelto" (Alpine.js, expansión 100% client-side reusando el modelo de confianza que ya existía para `price_override`); `_billing_summary.blade.php`/`_work_order.blade.php`/reportes PDF/email actualizados para no asumir `->service` en cada línea. Guardas nuevas en `ItemController`/`ServiceController::destroy()` contra el `restrictOnDelete` (error amigable en vez de 500). **Deliberadamente fuera de alcance:** Hotel/Hospital (sin campo de precio ni pantalla de facturación hoy — fase 2 separada), consumo automático de inventario de componentes de Grupo (la cotización es una "prefactura" que no debe afectar inventario/contabilidad hasta un concepto de "factura → cuenta por cobrar" formal que no existe todavía en el sistema — diseñarlo es su propio proyecto futuro), revivir el dominio `Execution`/`ExecutedService`/`Stay` (confirmado código huérfano, no bindeado, no invocado). "Grupos" vive en navegación bajo Catálogos (junto a Servicios), permiso nuevo `catalogo_grupos`. 17 tests nuevos (`GroupTest`, `GroupComponentTest`, `QuoteGroupTest`, `AccountingServiceItemLinesTest`), suite sin regresiones (37 fallidas preexistentes, 168 pasan). Verificado end-to-end en producción real dentro de transacciones revertidas | 15/07/2026 | `1251015` |
| BL-056 | Foto de artículo (`items.photo_path`) — parte de BL-052 que sí se hizo ya ("te falta la foto del producto"), separada de la automatización de catálogo de WhatsApp/redes que sigue pendiente (ver BL-052). `ItemPhotoImageManager` nuevo, mismo patrón exacto que `OperatorPhotoImageManager` (recorte cuadrado vía `x-image-upload`, original + thumbnail, `Spatie\Image`). Miniatura visible en el listado de Artículos y en la tabla de componentes de un Grupo (ayuda a identificar el producto al armar una cotización desglosada). Config nueva `backoffice.images.items` | 15/07/2026 | `1251015` |
| BL-057 | Costo de compra + precio de venta sugerido en Artículos — `items.cost_price` nuevo, y sección de configuración nueva "Tienda y Proyectos" (`SystemSettings` → `store_profit_margin_percentage`, default 30%) con el margen de utilidad usado para sugerir `price` (`costo × (1 + margen/100)`). Botón "usar sugerido" en el form (Alpine.js) — cálculo visible en vivo mientras se captura el costo, pero **no fuerza** el precio final, sigue siendo editable a mano. Mismo patrón genérico de secciones de `SystemSettings` ya usado por Cobertura Geográfica/Clínica — cero código nuevo en el renderizado, solo se agregó la definición de la sección. 1 test nuevo, suite sin regresiones (37 fallidas preexistentes, 169 pasan) | 16/07/2026 | `20787c2` |
| BL-058 | Toggle real de módulo "Tienda" (Artículos/Grupos) — motivado por Zeus-Estetican: hasta hoy solo Clínica tenía un interruptor de verdad (`clinical_module_enabled`), así que su catálogo de "Módulos" togglables por cliente era mayormente cosmético fuera de Clínica. `store_module_enabled` nuevo en `SystemSettings` (sección "Tienda y Proyectos", **default `true`** — a diferencia de Clínica, Tienda ya está en uso real en producción, así que nace prendida para no regresionar la instalación actual). Middleware `EnsureStoreModuleEnabled` (alias `store.module`, mismo patrón que `clinical.module`) envolviendo `items.*`/`groups.*`/`item-movements.*`; gateo de navegación en `InventoryNavigation`/`CatalogsNavigation`; el quote manager de Spa (`_quote_manager.blade.php`) oculta "Agregar grupo completo"/"Agregar artículo suelto" cuando está apagado sin afectar "Agregar servicio adicional" (Servicios sigue siendo core); guarda de backend en `storeQuote()` que rechaza líneas `item_id`/`group_id` si el módulo está apagado (defensa además del gateo de UI). Decisión de arquitectura confirmada con el usuario: **Estética deja de ser candidata a toggle** — es el kernel del que cuelgan Clínica y Hotel, pasa a ser Core fijo en el catálogo de Zeus; **Hotel queda fuera de esta entrega** (está fusionado dentro del calendario compartido de Agenda, mucho más invasivo de aislar). **Bug real descubierto y corregido en el mismo cambio** (ver NT-038): un `@json()` con array multilínea anidado en `_quote_manager.blade.php`, latente desde BL-055, generaba `ParseError` real en producción en cuanto Blade recompilaba la vista — nadie lo había detectado porque ningún test previo hacía `GET` a `agenda.show`; se corrigió moviendo el armado del array a `SpaBookingController::show()` (`$groupsForQuoteManager`). 9 tests nuevos (`StoreModuleToggleTest`), suite sin regresiones (37 fallidas preexistentes, 178 pasan) | 16/07/2026 | `f0460f8` |
| BL-049 | Módulo Tienda/Inventario real, completo en 3 piezas (ver `docs/tecnico/MODELO_BD.md` para el detalle técnico de cada una): **(1)** "IM sencillo" — ledger `item_movements` (BL-054, 15/07/2026); **(2)** multi-sucursal real — `item_branch_stocks`, saldo cacheado por (item, sucursal) (17/07/2026); **(3)** descuento por venta cobrada al completarse una cita (`BookingStockConsumptionService`, commit `46845c0`) + transferencias entre sucursales (`ItemMovementService::transfer()`, par de movimientos ligados sin tabla `warehouses` nueva) (18/07/2026). 11 tests nuevos en la pieza 3 (`BookingStockConsumptionTest` ×5, `ItemTransferTest` ×6), suite sin regresiones (37 fallidas preexistentes, 203 pasan) | 18/07/2026 | `a21cc46` |
| BL-052b | Categoría de producto de Meta/Google + variantes de color en Artículos — motivado por el mismo artículo de prueba de BL-052 ("ID TAG Colores 38mm" en realidad se vende en 8 colores, y no había forma de capturar categoría). 3 columnas nuevas en `items` (`meta_category`, `meta_variant_group`, `meta_color`, migración aditiva) — sin tabla nueva: cada color es su propia fila de `Item` (ya es una entidad completa con stock/precio/foto propios), enlazadas por un texto compartido que se manda tal cual como `retailer_product_group_id` de Meta. `MetaCatalogSyncService::buildPayload()` agrega `category` cuando está presente, y `retailer_product_group_id`+`color` solo si ambos `meta_variant_group`/`meta_color` están presentes (regla de omisión suave, no salta el artículo del sync). **Verificado contra la API real el 20/07/2026** (artículo #10, renombrado "ID TAG 38mm — Negro"): Meta aceptó y devolvió los 3 campos tal cual, incluyendo el `&` de la taxonomía de Google. 5 tests nuevos (`MetaCatalogSyncTest`, 12 en total). `size` quedó fuera a propósito (sin necesidad real hoy), extensible después con el mismo patrón. **Cerrado el 20/07/2026:** se completó el conteo físico de los 7 colores restantes (Azul Oscuro, Dorado, Azul Turquesa, Magenta, Morado, Verde, Rojo — altas #11-17, misma foto/precio/costo que el Negro) y se confirmó en Commerce Manager que Meta agrupa las 8 variantes como un solo producto colapsable ("Variantes: 8") bajo `retailer_product_group_id=id-tag-38mm`, no como productos sueltos. **Incidente no relacionado al código, resuelto en el camino:** el primer intento de sync tras dar de alta los 7 colores nuevos falló con "API access blocked" (OAuthException code 200) en los 8 artículos — causa raíz: `developers.facebook.com` bloqueó la cuenta de desarrollador por "actividad inusual", exigiendo confirmación de identidad antes de aceptar cualquier llamada de API de esa app. Resuelto por el usuario confirmando la cuenta manualmente; el sync funcionó sin cambios de código en cuanto se levantó el bloqueo (ver NT-040). | 20/07/2026 | — |
| BL-052 | Automatizar publicación en catálogo de WhatsApp Business (Meta Commerce Manager) usando existencias reales — integración real vía Meta Catalog API. `App\Domain\MetaCatalog\Services\MetaCatalogSyncService`, sección `SystemSettings` → "Catálogo de WhatsApp/Meta" (Catalog ID `2121927028717553` + Access Token cifrado, usuario de sistema "Sync Catalogo Backoffice", permiso `catalog_management`), botón manual "Sincronizar catálogo WhatsApp" en Artículos (sin scheduler/cron). Filtra `Item` por `is_active`+`ai_visible`+`stock_quantity > 0` (mismo filtro del asistente IA), omite sin foto/precio, `POST /{catalog_id}/products` por artículo con `retailer_id = "item-{id}"` (upsert, sin columna nueva para trackear el ID de Meta). **2 bugs reales encontrados y corregidos en la primera prueba contra la API real** (ver `MODELO_BD.md`): `price` debía ir como entero en centavos, no como string `"150.00 MXN"` (`(#100) Param price must be a number`); `Item::getPhotoUrlAttribute()` devolvía ruta relativa, invisible hasta que un consumidor externo (Meta) necesitó URL absoluta para descargar la imagen. Primer artículo real (#10 "ID TAG Colores 38mm") publicado con éxito. 7 tests (`MetaCatalogSyncTest`) | 20/07/2026 | — |
| BL-059 | Toggle real de módulo "Hotel" (Hospedaje) — continuación directa de BL-058, el candidato que había quedado descartado por "mucho más invasivo": Hotel no tiene sección propia en la navegación, está fusionado dentro del calendario/timeline compartido de Agenda, del dashboard y del selector de creación de citas. `hotel_module_enabled` nuevo en `SystemSettings` (sección "Hotel (Hospedaje)", **default `true`**, mismo razonamiento que Tienda — ya está en uso real). Middleware `EnsureHotelModuleEnabled` (`hotel.module`) envolviendo `hotel-reservations.*`; `SpaBookingController::index()`/`buildCalendarRange()` dejan de consultar `HotelReservation` cuando está apagado (la Agenda unificada queda solo con SPA); `DashboardController` oculta el KPI "Huéspedes en Hotel" y su acceso rápido; `agenda/global-create.blade.php` oculta la tarjeta "Hospedaje (Hotel)"; el copy fijo de `agenda/index.blade.php` que mencionaba "SPA y Hotel" pasa a condicional. **Bug real descubierto y corregido en el mismo cambio** (ver NT-039): las tres consultas que buscaban "huéspedes activos" filtraban por `status = 'active'`, un valor que **no existe** en el enum real de `hotel_reservations` (`scheduled`/`cancelled`/`fulfilled`) — el KPI del dashboard y la fusión de Hotel en la Agenda unificada llevaban rotos (siempre en cero) desde que se construyeron, sin que nadie lo notara porque "0 huéspedes" es un resultado creíble a simple vista. Se corrigió a `status = 'scheduled'` (el estado real que usa todo el resto del módulo) y se acotó el KPI del dashboard a la fecha de hoy, que antes no filtraba por fecha en absoluto. 8 tests nuevos (`HotelModuleToggleTest`), suite sin regresiones (37 fallidas preexistentes, 186 pasan) | 16/07/2026 | `2cfafd6` |
| BL-060 | Horario de trabajo semanal recurrente + bloqueos de no-disponibilidad (vacaciones/permisos) por operador — el usuario pidió revisar si esto ya existía; no existía nada (ni horario personal ni ausencias), solo el horario operativo *general* del negocio (`BusinessHours`, único y global) y el traslape de citas por operador (`OperatorAvailabilityChecker::hasConflict()`, BL-025). 2 tablas nuevas: `operator_weekly_schedules` (día 0-6 alineado a `Carbon::SUNDAY..SATURDAY` + hora inicio/fin, único por operador+día) y `operator_unavailabilities` (rango datetime + motivo libre). **Decisión clave de compatibilidad, confirmada con el usuario:** el horario semanal es opt-in — un operador sin ninguna fila capturada sigue agendable a cualquier hora (cero regresión); las no-disponibilidades **no son opt-in**, si existen y se traslapan siempre bloquean. 2 métodos nuevos en `OperatorAvailabilityChecker` (`isOutsideWorkingHours()`, `hasTimeOff()`, sin interfaz, mismo patrón que `hasConflict()` ya existente) integrados en los 4 puntos reales donde se valida `operator_id` al agendar/reprogramar (`Api\BookingController::store()/update()`, `SpaBookingController::update()/storeForPet()`), mismo bloqueo duro (422/redirect+flash) que el traslape de citas, no solo aviso (a diferencia de BL-043 cobertura geográfica). Captura exclusiva desde Backoffice web en la ficha del operador (tabla de 7 días en el formulario existente + sección de bloqueos con alta/borrado); la app móvil y el propio operador no la tocan. Sin permiso Spatie nuevo (consistente con que `operators` hoy tampoco tiene uno). **Reincidencia real de NT-010** (bloque `@php...@endphp` multilínea dentro de un `@foreach` — bug de compilador Blade ya documentado en el proyecto): la tabla de 7 días la disparó de inmediato ("Undefined variable $dayLabels"), corregido usando la forma de una sola línea `@php($var = expr)` que la propia NT-010 recomienda. **Bug real encontrado probando en producción real** (primer intento del usuario tras el cierre inicial): la tabla nacía vacía/sin marcar por operador, así que capturar horas sin marcar la casilla "Trabaja" guardaba la cita del operador pero cero filas de horario, sin ningún error visible — confuso para el usuario ("no guarda los cambios"). Corregido a pedido del usuario: el formulario ahora precarga los 7 días marcados con el horario operativo general del negocio (`BusinessHours`) en cuanto el operador no tiene ninguna fila capturada — deja de partir vacío, el staff ajusta o desmarca desde ahí en vez de armar desde cero. Los 2 operadores reales existentes se corrigieron con un backfill de una sola vez (7 filas c/u = horario general, verificado en la BD real de producción, no solo en tests). 20 tests nuevos en total (incluye 2 de esta segunda pasada), suite sin regresiones (37 fallidas preexistentes, 235 pasan — antes 215) | 20/07/2026 | — |
| BL-061 | Autoservicio real para que un operador bloquee sus propias horas desde la **app móvil**, sin depender del administrador — continuación directa de BL-060, a pedido del usuario. Decisiones confirmadas: vive en `mob_apps/operador` (no Backoffice web) y el permiso solo deja tocar **el propio** registro de `Operator`, nunca el de otro. Permiso nuevo `disponibilidad_propia` agregado al array `$modules` de `BaseRolesSeeder` (genera `ver/crear/editar/eliminar disponibilidad_propia`) y a la misma matriz de checkboxes que ya usa `Usuarios → editar` (`UserController@create()`/`@edit()`) — **se descartó a propósito un permiso granular suelto tipo `disponibilidad.administrar`**: la matriz de permisos de Usuarios hace `syncPermissions()` con solo lo marcado ahí, así que cualquier permiso fuera de esa matriz se borraría solo en la siguiente edición del usuario (mismo mecanismo por el que `alergias.administrar`/`clinico.firmar` etc. solo se asignan vía rol, nunca directo — ver NT-041). Sin rol operador genérico nuevo — se asigna el permiso directo al usuario desde esa misma matriz. Controller API nuevo `Api\UnavailabilityController` (`index`/`store`/`destroy`, JSON) que resuelve siempre `$request->user()->operator` (relación `User::operator()` ya existente vía `users.operator_id`) — nunca lee `operator_id` del body, así que aunque alguien lo mande en el payload de `store()`, la fila se crea ligada a su propio operador; `destroy()` compara `operator_id` antes de borrar. 3 rutas nuevas bajo `/api/me/unavailabilities` (primer uso de `middleware('permission:...')` en `routes/api.php`, funciona sin fricción porque `ApiAuthenticate` hace login sobre el guard `web`, el mismo que usa Spatie). Pantalla nueva `MobUnavailability.tsx` (lista + FAB + bottom-sheet de alta, mismo patrón visual que `MobCaja.tsx`; borrado real por fila vía `DELETE`, no solo en memoria), enlazada desde una sección nueva en `MobUserConfig.tsx` visible solo si `user.operator_role` existe (limpieza de UI, no es el gate de seguridad real — ese lo resuelve el servidor). 8 tests nuevos (incluye verificación de que `BaseRolesSeeder` sigue corriendo limpio), suite sin regresiones (37 fallidas preexistentes, 243 pasan — antes 235). App móvil verificada con `tsc --noEmit` + `npm run build` (sin infraestructura de test de UI en este entorno) — los 2 errores de `tsc` que aparecen son preexistentes y ajenos (`ActiveService.tsx`, huérfano documentado en BL-037; tipo `key` en `MobCajaMovimientos.tsx`). Permiso corrido en producción real vía `db:seed --class=BaseRolesSeeder --force`. **Corregido en producción real el mismo día:** al probar, el único usuario operador real (`tomasmg`) tenía marcado por error el checkbox "Editar" en vez de "Ver"/"Crear"/"Eliminar" en la matriz de Usuarios — corregido directo en la BD (acción aditiva, sin quitar permisos existentes). **Ajuste de UX pedido por el usuario tras probarlo:** enterrar la función en Configuración personal no era práctico — se agregó como segunda opción en el menú "+" de la Agenda móvil (`GlobalAgenda.tsx`, junto a "Nueva cita", visible solo si `operator_role`), y se le agregó un paso de confirmación con contraseña antes de crear el bloqueo (reutilizando `POST /api/me/verify-password`, el mismo endpoint ya usado para desbloquear la app en BL-038 — no cambia contraseña ni token, solo confirma identidad) para evitar bloqueos accidentales de horario. | 20/07/2026 | — |
| BL-062 | Mostrar horas bloqueadas (`operator_unavailabilities`) en la Agenda — móvil **y** web — y dejar de ofrecerlas como disponibles al crear una cita. El usuario probó agendar en un día bloqueado y la Agenda solo decía "sin citas", sin ninguna pista de la no-disponibilidad. Hallazgo clave: hasta hoy **nada leía** los bloqueos salvo el rechazo 422 al guardar (BL-060) — había 3 fuentes de "qué hay agendado" totalmente desincronizadas (`OperatorAvailabilityChecker::hasConflict/hasTimeOff` solo al guardar, `Api\AgendaController::index()` para agenda/selector móvil, `SpaBookingController::buildCalendarRange()`/`index()` para agenda web), ninguna tocaba `OperatorUnavailability` para lectura. Se agregó **un solo método compartido** (`OperatorAvailabilityChecker::unavailabilityWindows()`, reusa el `scopeOverlapping` ya existente) consumido por los 3. **Móvil**: nuevo endpoint `GET /api/agenda/unavailabilities` (mismos params `view`/`date`/`operator_id` que `/api/agenda`, refactor de `resolveRange()` compartido); `GlobalAgenda.tsx`/`GroomerAgenda.tsx` pintan un banner "Operadores no disponibles" en vista Día (siempre visible si aplica, no solo cuando no hay citas) y un punto/ícono distinto de los de estado de cita en semana/mes (`AgendaCalendarGrid.tsx`, prop `blockedDates` nueva, sin tocar `STATUS_DOT`); `MobCitaNueva.tsx` excluye los slots bloqueados del grid de horario (4to estado visual "Bloqueado", distinto de "Ocupado", mismo patrón `buildSlots` ya usado para citas). **Web**: como el Backoffice nunca tuvo un selector visual de horarios (solo `<input type="datetime-local">` + validación al submit), en vez de construir un grid nuevo desde cero se extendió el `<script>` vanilla JS que ya existe en `create.blade.php`/`edit.blade.php` (togglea `.is-locked` según operador) para hacer un chequeo en vivo contra un endpoint nuevo (`GET /agenda/check-availability`, sesión web normal) que reusa **los 4 checks ya existentes** (`BusinessHours::isWithin`, `hasConflict`, `isOutsideWorkingHours`, `hasTimeOff`) sin duplicar lógica — muestra una advertencia inline, no bloquea el submit (el 422 real sigue siendo el guardrail duro). Vista Día web gana el mismo banner que móvil; semana/mes ganan un punto gris nuevo (`bandeja-calendar-dot--bloqueo`, reusando la clase base ya creada para Bandeja Diaria de WhatsApp en BL-030, portada por primera vez a la Agenda general). 18 tests nuevos (lectura de ventanas por vista/operador, los 4 casos de `check-availability` + exclusión de la propia cita al reprogramar, banner/dot en las 3 vistas web), suite sin regresiones (37 fallidas preexistentes, 257 pasan — antes 243). App móvil verificada con `tsc --noEmit` + `npm run build`, mismos 2 errores preexistentes de siempre | 20/07/2026 | — |
| BL-063 | Fix + feature de bloqueo automático de sesión (`AppLockContext.tsx`, BL-038): el usuario reportó que a veces la app se bloqueaba sola al solo cambiar de pantalla (navegación normal, no cambio de app real). **Causa raíz encontrada:** el listener `visibilitychange` bloqueaba de inmediato (`setLocked(true)`) en cuanto `document.hidden` era `true`, sin ningún margen — algunos WebView de Android disparan `hidden` momentáneamente durante navegación interna o al abrir un picker nativo (fecha, foto) sin que el usuario haya salido de verdad de la app. Corregido con un margen de gracia (`HIDDEN_GRACE_MS = 1500`): al ocultarse se arma un timer corto; si la pestaña vuelve a ser visible antes de que se cumpla (típico de un picker nativo), se cancela y no bloquea; un cambio real de app dura muchísimo más que ese margen, así que sigue bloqueando en ese caso. **Segundo pedido del usuario, mismo cambio:** el timeout de inactividad (antes fijo, `5 * 60 * 1000` en código) ahora es **configurable por usuario** — nuevo campo `lockTimeoutMinutes` en `useUserPrefs` (mismo patrón 100% cliente/`localStorage` que ya usan tema y breadcrumbs — es una preferencia de dispositivo, no de cuenta en el servidor, consistente con que el candado de sesión en sí es inherentemente por-dispositivo), selector nuevo (1/2/5/10/15/30 min) en Configuración personal → Seguridad, junto al toggle de biométricos ya existente. Sin backend tocado — cambio 100% en `mob_apps/operador`. Verificado con `tsc --noEmit` + `npm run build` (sin infraestructura de test de UI en este entorno) | 21/07/2026 | — |
| BL-064 | Preseleccionar al operador logueado en "Nueva cita" (`MobCitaNueva.tsx`) — a pedido del usuario, para que un operador vea de una vez su propia disponibilidad en vez de tener que elegirse a sí mismo cada vez. `User::toApiArray()` ganó `operator_id` (antes solo exponía `operator_role`, el nombre del puesto, sin el FK real hacia `Operator`) — necesario porque `AuthContext`/`GET /api/me` es la única fuente del operador vinculado al usuario logueado en el cliente. Nuevo efecto en `MobCitaNueva.tsx`: en cuanto llega la lista de operadores, si el usuario tiene `operator_id` y aparece en esa lista, se preselecciona sin pisar una selección manual ya hecha. Quien no es operador (admin puro) no ve cambio de comportamiento. 2 tests nuevos (`ProfileTest`), suite sin regresiones (37 fallidas preexistentes, 259 pasan — antes 257). App móvil verificada con `tsc --noEmit` + `npm run build` | 21/07/2026 | — |
| BL-065 | Asignar el permiso `disponibilidad_propia` (BL-061) dejó de ser una fila confusa de 4 checkboxes CRUD en la matriz de Usuarios — a pedido del usuario, "hacerlo más fácil de asignar". Se quitó `disponibilidad_propia` del array `$modules` genérico (en `create()`/`edit()` de `UserController`) y se reemplazó por un **switch único** ("Puede bloquear su propia disponibilidad") en la tarjeta "Perfil de Operador" del formulario de Usuario — activarlo otorga `ver/crear/eliminar disponibilidad_propia` juntos (no existe acción de "editar" un bloqueo, solo alta/borrado, ver NT-041), desactivarlo los revoca, sin tocar el resto de permisos ya marcados en la matriz CRUD normal (`store()`/`update()` arman el array de `syncPermissions()` fusionando ambas fuentes antes de guardar). De paso se unificó una inconsistencia real preexistente entre `store()` (no sincronizaba permisos si el campo `permissions` venía vacío) y `update()` (los borraba todos) — ahora ambos sincronizan siempre, con el mismo comportamiento. 5 tests nuevos (`UserOwnAvailabilityToggleTest`: switch por default apagado, otorga/revoca los 3 permisos, no pisa otros permisos de la matriz, funciona también en alta de usuario nuevo), suite sin regresiones (37 fallidas preexistentes, 264 pasan — antes 259). Verificado contra producción real que el permiso ya asignado a `tomasmg` (corregido en BL-061) sigue intacto | 21/07/2026 | — |

---

## Reglas de gestión

- Un ítem **no se elimina** — se mueve a Completados con fecha y referencia de commit.
- Si un ítem genera una NT, agregar referencia en la columna Notas.
- Si un ítem se descarta (won't do), moverlo con motivo documentado.
